# frozen_string_literal: true
require 'spec_helper'

describe Pronto::Commentator do
  it 'has a version number' do
    expect(Pronto::Commentator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
